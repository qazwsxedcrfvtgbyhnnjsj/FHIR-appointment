## 🛠 Slot 流程（名額管理）

1. 管理員建立活動 Schedule（排程）
   - 定義活動是哪一天、哪個時段
2. 系統根據 Schedule 自動產生一或多個 Slot（表示可被預約的名額）
   - 例如：同一場活動有 30 個名額 → 產生 30 個 Slot
3. 使用者報名時：
   - 系統檢查該 Slot 是否還有可用（status 為 `free`）
4. 報名成功：
   - 將一個 Slot 的狀態從 `free` 更新為 `busy`
   - 建立 Appointment 並 reference 到該 Slot
5. 管理員後台可查看：
   - Slot 剩餘名額（status）
   - 報名者對應到的 Slot
6. 若有取消報名：
   - 系統將對應的 Slot 狀態改回 `free`
   - Appointment 狀態改為 `cancelled`

---

## 📊 Mermaid 流程圖

```mermaid
flowchart TD
    A[管理員建立 Schedule] --> B[系統產生 Slot]
    B --> C[使用者發送報名請求]
    C --> D{Slot 是否可用?}
    D -- 是 --> E[建立 Appointment]
    E --> F[將 Slot 狀態設為 busy]
    F --> G[報名完成]
    D -- 否 --> H[提示名額不足]
    G --> I[管理員後台查看名額狀態]
    I --> J{如有取消報名?}
    J -- 是 --> K[Appointment 改為 cancelled]
    K --> L[Slot 狀態改回 free]
