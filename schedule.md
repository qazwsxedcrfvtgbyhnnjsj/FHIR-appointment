## 🛠 Schedule 流程（活動排程管理）

1. **管理員登入後台**
   - 查看所有既有活動排程（Schedule）
2. **新增活動排程（Schedule）**
   - 輸入活動名稱、負責人（actor）、日期時間範圍（planningHorizon）
   - 選擇主辦單位（Organisation）
3. **系統建立 Schedule 資源**
   - 記錄活動時段與對應負責單位
4. **系統自動產生對應的 Slot（名額）**
   - 依活動名額需求產生多個 Slot（狀態：free）
5. **使用者報名流程**
   - 報名時系統會 reference 到對應的 Schedule 與 Slot
6. **管理員後台可隨時調整：**
   - 修改活動排程時間（Schedule.planningHorizon）
   - 增減 Slot 名額
   - 停用或取消整個活動排程

---

## 📊 Mermaid 流程圖

```mermaid
flowchart TD
    A[管理員登入後台] --> B[新增/編輯 Schedule]
    B --> C[系統建立/更新 Schedule]
    C --> D[系統產生 Slot（名額）]
    D --> E[使用者開始報名]
    E --> F[報名 reference 到 Schedule & Slot]
    C --> G[管理員後台查看/修改排程]
    G --> H[如需修改時段或名額]
    H --> I[更新 Schedule.planningHorizon 或 Slot 數量]
ㄏ 
